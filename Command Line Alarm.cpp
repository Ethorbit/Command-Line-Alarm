#include <string>
#include "input.h"
#include "alarm.h"

int main(int argc, std::string argv[])
{
	// If too few arguments passed or they are not running through the command line:
	if (argc < 3) {
		alarm Alarm("10:30AM", L"G:\\Desktop\\Jeff\\Pew pew lame sound effect.wav");
		//input getInput;
	} 
	else {
		
	}
}