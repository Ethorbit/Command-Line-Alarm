#include <string>
#include "input.h"
#include "alarm.h"
#include "timer.h"

int main(int argc, std::string argv[])
{
	// If too few arguments passed or they are not running through the command line:
	if (argc < 3) {
		input getInput;
	} 
	else {
		//timer Timer(argv[0], std::wstring(argv[1]));
	}
}